name: flutter_asistente_memoria
description: APP Asistente a la Memoria

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_bloc:
  equatable:
  formz: ^0.4.0-nullsafety.0
  firebase_core: "^1.0.3"
  firebase_analytics: "^8.0.0"
  firebase_auth: "^1.0.3"
  cloud_firestore: "^1.0.5"
  shared_preferences:

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
  assets:
    - assets/

        
    
    
                                      